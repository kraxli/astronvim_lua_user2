return {
--   follow_current_file = false,
  filesystem = {
    follow_current_file = false, --true,
    hijack_netrw_behavior = "disabled", -- "open_current",
  },
  buffers = {
    follow_current_file = false, --true,
  },
}
